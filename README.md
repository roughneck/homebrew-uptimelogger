# homebrew-uptimelogger

A Homebrew tap for [UptimeLogger](https://uptimelogger.bartels.ug/), a free
connection monitor for Windows and macOS.

```
brew tap roughneck/uptimelogger
brew install --cask uptimelogger
```

This tap contains the cask only. It is not the application's source repository.
